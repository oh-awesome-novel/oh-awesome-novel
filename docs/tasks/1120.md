# 1120 Play World Events And Turn Settlement

> Status: Needs Review
> Milestone: Play Mode

## Related Plans

- [Play Mode World Events Upgrade Plan](../PLAY_MODE_WORLD_EVENTS_UPGRADE_PLAN.md)

## Goal

让 Play Mode 不只追加角色对话，而是通过单一 world referee 在同一 Play-local 回合中结算叙事、时间、外部事件、状态变化、observation 和行动建议，同时保持 canonical truth 与 Human Approval 边界。

## Implemented Slice

- Play session schema v4：保留 `revision`、world clock、event policy、typed world events、suggested actions，并把结构化回合事实迁移到 `turns/<turn-id>.yaml`。
- `session.yaml` 只保存 metadata、`selectedTurnIds` 和必需的 `branchSnapshotRequiredFromRevision` / `branchBaseSnapshot`，不再保存完整 transcript；`transcript.md` 和 Core 暴露的内存 transcript 均从 selected turn path 单向投影，手工修改 projection 不会反向成为事实。
- settlement 与手工 transcript mutation 都会生成 turn artifact；artifact 可携带 parent、输入、消息、clock、event / observation refs、state delta、suggested actions 和 `canonical: false`。
- turn graph、selected root-to-head path、artifact / message / event / observation / adoption candidate 唯一性和跨 ledger 引用采用 fail-closed 校验；artifact schema v2 + session legacy cutoff 防止把新回合降级伪装成 legacy，严格 `artifactKind` 区分 settlement 与普通 transcript append。版本化 branch snapshot 同时封存 world clock、Play-local state value / visibility、suggested actions、完整 schedule head 与 hard-due evidence；v4 的 branch base 为 root 或 legacy bridge 提供可验证前驱。referee prompt、cause validation、feed 与 scheduled-event evaluator 只读取 selected projection，不能信任被篡改的内存 transcript 或 sibling branch snapshot。ledger 可保留 provenance 连贯的兄弟分支历史，但手工新建 observation / candidate 只能引用当时 selected branch，UI 也只投影当前 selected facts。
- committed turn artifact 已作为隐式 checkpoint 暴露；恢复通过 mandatory `baseRevision`、同 session lock 和 staged snapshot 切换 selected root-to-head path，同时恢复 transcript、world state / visibility、schedule 与 suggestions。session revision 保持单调，全部 artifact / event / observation / adoption ledger 保留；恢复后新回合会从目标 head 形成 sibling variant，旧后续回合可再次选择。checkpoint preview 只使用用户输入，不旁路泄露 referee / hidden narrative。
- v1 / v2 session 可兼容读取为 legacy turn artifacts；空的 v3 session 或 structured turns 仍为 artifact v1 且 projection 可验证的 v3 session 也可显式迁移。Core migration preview 会报告生成的 turn id、未知 metadata key 和备份位置。第一次写入 v4 时，原始 session 与 preview 一并进入 `.migrations/v1-to-v4/`、`.migrations/v2-to-v4/` 或 `.migrations/v3-to-v4/`，未知顶层 session metadata 被保留，migration history 在后续 staged save 中继续保留。升级时封存当前 selected legacy projection 为 `branchBaseSnapshot`，并以当前 revision 作为 cutoff；内含 artifact v2 但缺少两项证据的 v3 session 仍 fail closed。
- `events.yaml` 保存结构化 Play-local world events；`event-schedule.yaml` 保存 typed scheduled event、trigger、状态与 resolution evidence。旧 session 缺少事件或 schedule 文件时按兼容空值读取。
- 宿主已提供确定性的 `nextTurn` / `afterTurns` / `flagEquals` hard-due evaluator；`atWorldTime` 只有显式规范化比较器时启用，`manual` 不自动触发。到期事件必须由 settlement 以 `triggerId` 恰好结算一次，并且不受 spontaneous event budget 限制。
- referee 可通过 typed `scheduledEventChanges` 创建、取消或改期未来事件；宿主生成 schedule id、revision、turn 与 source evidence，schedule 与 turn / state / events 在同一 staged snapshot 提交。
- world referee prompt 装配最近 transcript、Play-local state、事件、world clock、policy 与 activated source 实际内容。
- referee 只能使用 read tools；不能在 Play 回合中直接调用 canonical write-intent tools。
- `oan-play-settlement` 结构化结算协议：正文与 JSON settlement 分离，宿主生成 turn/event id、sequence、timestamp 和 `canonical: false`。
- settlement block 为必需协议；无效 settlement、超预算事件和伪造 cause reference 在写入前失败，用户输入、referee 文本和状态均不会产生部分提交。
- session snapshot 先写 sibling staging directory，再通过 directory swap 提交；读取时可恢复带 `.ready` 标记的中断事务。
- 所有同 session Play-local mutation 共享互斥锁与可选 `baseRevision` compare-and-swap，不再只锁 world turn。
- 显式重复 session id、未来 schema version 和 workspace 外 symlink source 会被拒绝。
- client 已接通 world-referee endpoint。
- Play 专用 turn SSE 已接通 `started -> context.ready -> narrative.delta* -> (narrative.reset -> narrative.delta*)* -> prepared -> committed / cancelled / failed`；响应头预先暴露 turn id 供首帧前断流对账，中间 read-tool loop 使用 reset 清除旧 provisional block。provider response 会隔离到发现 settlement boundary 后才检查并释放纯 prose narrative prefix；无 fence、合法 fence 前的 raw JSON / settlement 字段、额外 fence或原始 marker 都不产生 provisional delta / commit，失败 / conflict 会撤回未提交 provisional，只有 committed session 才能替换权威状态。
- durable snapshot 成功后，Backend 会在 terminal `play.turn.committed` 前发布逐项 `play.event.occurred`；cancel / validation / provider / commit failure 不发布。`play.turn.committed.session` 仍是 transcript、world state、schedule、observation 与 event feed 的权威 UI 刷新载荷。
- 服务端使用绑定不可变 workspace 的 turn registry、显式 cancel endpoint 与 commit barrier 处理 stop / cancel 竞态；活跃回合期间拒绝 workspace create / open / remove，commit 开始前取消不会推进 revision，commit 已开始后会明确返回 too-late / committed truth。
- Desktop UI 已提供真实 provisional block、Stop、取消/失败/冲突/结果未知状态与独立 `aria-live` 状态播报；发送期间锁定 session 切换、刷新和 adoption mutation，对账 transport 失败时不谎报“未提交”，而是要求刷新服务端事实后再继续。workspace mode 与工作台布局按 workspace 恢复，session rail 具备明确的展开和当前项 ARIA 语义。
- 顶级独立 Play workspace：session rail、transcript、action composer、world clock、event feed、spoiler toggle、state/source HUD、observation/adoption。
- v4 state visibility 必须精确覆盖 state 叶路径，observation / candidate 必须显式声明 visibility；缺失、非法或危险路径会 fail closed。hidden turn 的 state / observation / candidate 与 `cause.reason` 使用 author-only projection；spoiler 关闭时不会从 HUD、event cause 或 adoption 泄露，但允许隐藏原因产生公开可感知的后果。
- adoption 使用目标化表单生成 candidate，携带去重的 observation / turn / event provenance，再显式创建 PendingAction；candidate id 由宿主生成，不接受调用方控制；不再暴露 raw JSON 作为主要体验。

## Remaining Review Scope

- 在现有 staged directory swap 基础上补齐 fsync、跨进程锁、逐故障点注入和长期残留 stage 清理。
- 将当前进程内、60 秒保留的 turn registry 升级为可重启恢复的 terminal truth，并让 backend graceful shutdown 主动取消可取消回合、等待 commit barrier 安全结束。
- 为 activated-source 读取、provider 生命周期和 SSE producer 增加端到端 deadline、bounded read / backpressure 与资源回收验证；当前输出字符有上限，但完整 committed session 和慢客户端仍等待 summary / windowing 收口。
- 将 Core migration preview 暴露到 backend / client / UI，并实现首次升级的显式确认、取消和错误恢复体验；当前 mutation 会在 staged snapshot 内完成迁移，但产品层尚无确认入口。
- 在已落地 scheduled hard-due evaluator 上继续增加 pressure、agenda、eligible event budget、规范化 `atWorldTime` contract、hidden reveal link 和更严格的 event/state delta 引用。
- 补齐命名 checkpoint、初始空世界 checkpoint 与同一 before-turn snapshot 上的原子 retry；当前已能列出 committed-turn 隐式 checkpoint、恢复 selected transcript / state / event projection / schedule 并切换既有 variant，但尚无独立 branch-local knowledge / reveal store，因此完整 knowledge restore 验收仍未闭环。
- 增加 Play context trace、omitted source 和 canonical source drift / rebase / fork。
- 将 session list 拆成 summary + selected detail，并为长 transcript / event feed 增加窗口化。
- 扩展根目录 `__test__/desktop-ui`：当前已覆盖 stream reducer、cancel / commit race、中性样式、workspace mode / layout 恢复、session rail mounted ARIA，以及 checkpoint list / restore composable、inline confirmation 与焦点返回；仍需补窗口化、checkpoint / variant 的完整 workspace / backend 旅程、全键盘旅程与浏览器级 smoke。

## Implementation Notes

### 2026-07-14

- `packages/core/src/play-turn-artifact.ts` 新增独立 turn artifact schema、legacy transcript 转换、selected path 校验与 transcript projection helper。
- `packages/core/src/play-session.ts` 当日首次引入结构化 session schema；staged snapshot 开始写入 `turns/`，并在同一 swap 中生成 projection 与迁移备份。
- `packages/core/src/index.ts` 当日导出 turn artifact、migration preview 与 artifact path API；`packages/client/src/index.ts` 同步当时的 public session shape。当前 v4 形状见 2026-07-15 记录。
- `__test__/core/src/play-session.test.ts` 覆盖 selected-path projection、图与 ledger 完整性、忽略被篡改的 transcript projection、结构化 settlement artifact、v1 / v2 / v3 migration preview / backup、未知 metadata 与 migration history 保留、未来 artifact schema 和不安全 artifact id 拒绝。
- `packages/core/src/play-narrative-stream.ts` 提供跨任意 provider chunk 边界的 settlement fence filter；`packages/agent` 将 AbortSignal 继续传到 Runtime / AI SDK。
- `packages/backend` 新增 Play turn SSE、turn registry、cancel endpoint 和 commit barrier；`play.narrative.reset` 使 read-tool loop 后的替代正文不会与旧 provisional 文本拼接；`packages/client` 新增 typed event parser / cancel API；Desktop UI 通过独立 composable 维护 provisional / terminal 状态，并只用 committed session 刷新 transcript、world state、observation 与 event feed。
- `__test__/backend` 覆盖成功流、mid-stream cancel、不服从 abort 的 provider 释放锁、workspace transition 拒绝、旧 turn 的单调 revision 对账与无效 settlement 零提交；`__test__/client` 覆盖分块 SSE、stream / cancel variant 载荷校验、提前退出 body cancel 和 cancel 路由；新建 `__test__/desktop-ui` 覆盖 terminal 幂等、cancel / commit race、首帧前与 commit barrier 断流对账、半开 stream、长 committing 轮询、transport 全断时的 indeterminate 状态、活跃连接锁和中性设计契约。
- 回归验证范围包括 Core / Agent / Backend / Client / Desktop UI build 与分层测试；当日任务仍为 `Needs Review`，因为事务耐久性、产品层迁移确认、完整 world evaluator、checkpoint / variant、drift / context trace、summary / windowing 与更完整的 Desktop UI 自动化尚未闭环。

### 2026-07-15

- `packages/core/src/play-event-schedule.ts` 新增严格 trigger / template / schedule record schema 与纯 `evaluatePlayDueEvents`；危险 dotted path、未知字段、重复 id、非法状态证据和无 comparator 的世界时间推断均 fail closed。
- `packages/core/src/play-session.ts` 将 `event-schedule.yaml` 纳入 v4 staged snapshot；prompt 注入稳定排序的 hard-due skeleton，settlement 通过 typed `scheduledEventChanges` 创建 / 取消 / 改期，遗漏、重复、未知或 template 不匹配的 `triggerId` 整回合零提交。hard-due event 不占 spontaneous event budget。
- turn artifact schema 升级为 v2，并增加 `artifactKind`、`branchSnapshotVersion: 1` 与 `dueScheduledEventIds`；同时保存 world clock、Play-local state value / visibility after-state、suggested actions 与完整 schedule head，嵌套 state 在 session / delta / snapshot 间深拷贝。完整 parent 或对齐的 branch base 会重放 hard-due evaluator；session projection 必须匹配 selected head，source / resolution 必须属于当前祖先路径，occurred event cardinality / template、revision、world turn、state delta、visibility、状态转换与 event cause 在写入和读取时闭环校验。
- session v4 必需 `branchSnapshotRequiredFromRevision` 与 `branchBaseSnapshot`；watermark 必须等于 base clock revision 且不超过 session revision，revision 高于 cutoff 的 artifact 必须携带完整 v2 snapshot。新 session 以 revision `0` 基准启动，legacy 升级则把当前 selected head 与 projection 锁定为 base；删除、降级或断开这份证据都会 fail closed。
- Client 对 SSE / cancel 以及 list / get / create / append / observation / adoption / non-stream turn 的普通 JSON 响应统一执行 runtime guard，并复核 session revision、artifact graph、selected path、v2 branch snapshot 与 schedule head；Desktop event / observation feed 只显示 selected artifact projection。
- Backend 仅在 durable snapshot 写入成功后发布 `play.event.occurred`，Client 对 event kind / origin、schedule 和 turn artifact 载荷执行严格运行时校验；Desktop HUD 显示 spoiler-aware pending schedule，并继续以 committed session 为权威投影。
- Core / Client 对 schedule transition 采用同一 fail-closed 规则：predecessor 不可消失，unchanged / cancelled / occurred 保持 plan，cancel / occur / reschedule / create 都必须绑定当前 world-referee 的 revision、turn 与理由证据；到期事件与 occurred event 必须恰好一一匹配。
- branch base 只接受 host-owned pending schedule seed；terminal 或带 source / change / resolution 的无 artifact 计划在 create、read、write 和 Client envelope 阶段一致拒绝，避免持久化后首回合才暴露 ghost evidence。
- v4 读取 / 写入要求 state visibility map 精确覆盖 state 叶路径，并要求 observation / candidate 显式 visibility；manual provenance 在创建时限定 selected branch，持久化 sibling branch history 则按全局存在且单分支连贯校验。Desktop 在 author view 关闭时同时隐藏 `cause.reason`，并按 selected branch 投影 observation / candidate。
- workspace mode 与 layout preference 已按 workspace 版本化持久化；Play session rail 补齐 `aria-expanded`、`aria-controls` 与 `aria-current`，并引入 Vue mounted / router 恢复测试。
- Core 已把原本集中在 `play-session.ts` 的公共领域类型、selected branch / ledger facts projection 与 scheduled-event history state machine 分别下沉到 `play-types.ts`、`play-session-facts.ts` 和 `play-event-schedule-history.ts`；`play-session.ts` 保持原 public interface 的兼容 facade，并从约 3,876 行降到约 1,979 行。后续 Pressure / Agenda、checkpoint / variant 与 context trace 必须沿这些 module seam 扩展，不再回填单文件。
- `play-turn-graph.ts` 在 turn ledger 上实现隐式 checkpoint summary 与 selected projection restore；Core 覆盖完整投影恢复、落盘重读、旧 ledger 保留、恢复后 sibling variant，以及 current / unknown / unsafe / legacy snapshot 边界。Backend / Client 提供 mandatory-CAS list / restore 契约，Desktop 以独立 composable 和纯展示控件呈现 Current / Selected path / Variant，并在恢复前明确说明旧后续回合不会删除。
- task 仍为 `Needs Review`：Pressure / Agenda、eligible evaluator、typed state-delta refs、命名 / 初始 checkpoint、原子 retry、branch-local knowledge、事务耐久性、migration confirmation、drift / context trace、summary / windowing 与浏览器级 Play 旅程尚未闭环。

## Done Criteria

- 一个 Play turn 以结构化 draft 验证后一次提交，不存在 transcript-only 或 state-only 半回合。
- 外部事件有 kind、origin、cause、visibility、turn id、sequence 和 world clock 证据。
- hidden event 默认不进入 player-visible event feed，作者可显式开启 spoiler。
- Play referee 只读 canonical sources，Play 结果只能经 adoption candidate -> PendingAction 进入真实文件树。
- stale revision 在调用模型前返回冲突；无效 settlement 不改变 session。
- 旧 session 可继续读取和升级，不要求批量破坏式迁移。
- Core、Backend、Client 和 UI build 验证通过。

## Constraints

- 不引入多 Agent runtime、后台自治 Agent、SQLite 事实源或重型 event sourcing。
- 不在 `packages/runtime` 中加入 Play 领域逻辑。
- 不复制 SillyTavern、InkOS 或 OpenTavern 的代码与 prompt。
- 不允许 Play 回合直接写 canonical 文件。
